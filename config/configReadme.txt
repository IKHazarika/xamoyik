This folder includes the following:

1. settings includes format for entering date, max description length of edits etc

2. triggers includes the categories, keywords, wikidata items that prompt the bot to add the tag
